{
  "github": {
    "release": true,
    "releaseName": "${version}",
    "assets": ["dist/hslayers/*.js", "dist/hslayers/*.d.ts", "dist/hslayers/*.map"]
  },
  "hooks": {
    "after:bump": "npm run build --prod"
  }
}